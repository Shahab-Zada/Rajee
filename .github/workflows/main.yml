name: Deploy Job concurrency
en:
 on: push
  branches:[main]

jobs:
  build:
   runs-on: ubunto-latest
   concurrency:
     group: deploy
     cancel-in-progress: true
    
    steps:
     - name: Checckout
       uses: actions/checkout@v6.0.2

     - name: Install Dependencies
       run: npm install

     - name: Build
       run: npm run build
       
     - name: Deploy App
       run: echo "Deploying APP"
     
     
     
